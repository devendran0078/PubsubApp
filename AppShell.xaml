<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PubsubApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PubsubApp.Views"
   
    Title="PubsubApp">

    <ShellContent Title="Publisher"
                  ContentTemplate="{DataTemplate local:PublisherPage}" />
    <ShellContent Title="Subscriber"
                  ContentTemplate="{DataTemplate local:SubscriberPage}" />
</Shell>
